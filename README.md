# Bitpowr-create2-task

## Installation
* Clone the repo to your PC
* Install its packages and the dependencies by running `yarn`
* Run `yarn compile` to compile the contracts
* Run `yarn test` to run the test script
